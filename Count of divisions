#include<stdio.h>
int main()
{
 int num,i,count=0;
 printf("Enter a positive number:");
 scanf("%d",&num);
for(i=1;i<=num/2;i++)
  {
   if(num%i==0)
     {
       count=count+1;
     }
  }
   count=count+1;
printf("There is %d divisons",count);
return 0;
}
